<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CarGame.Pages.MenuPage"
             BackgroundColor="Black"
             Shell.NavBarIsVisible="False">

    <Grid Padding="24" RowDefinitions="Auto,*,Auto" RowSpacing="18">

        <Frame Grid.Row="0"
               Padding="26"
               CornerRadius="22"
               HasShadow="True"
               BackgroundColor="#202020"
               BorderColor="#3A3A3A"
               HorizontalOptions="Center"
               VerticalOptions="Center"
               MaximumWidthRequest="360">
            <VerticalStackLayout Spacing="10" HorizontalOptions="Center">
                <Label Text="Traffic Escape"
                       FontSize="34"
                       TextColor="White"
                       HorizontalTextAlignment="Center" />

                <Label Text="Avoid cars • Collect coins"
                       FontSize="14"
                       TextColor="#CFCFCF"
                       HorizontalTextAlignment="Center" />

                <Label Text="{Binding HighScoreText}"
                       FontSize="14"
                       TextColor="#CFCFCF"
                       HorizontalTextAlignment="Center" />

                <Label Text="{Binding CoinsHeldText}"
                       FontSize="14"
                       TextColor="#CFCFCF"
                       HorizontalTextAlignment="Center" />

                <Label Text="{Binding SelectedCarText}"
                       FontSize="12"
                       TextColor="#AFAFAF"
                       HorizontalTextAlignment="Center" />
            </VerticalStackLayout>
        </Frame>

        <VerticalStackLayout Grid.Row="1"
                             Spacing="12"
                             VerticalOptions="Center"
                             HorizontalOptions="Center"
                             WidthRequest="260">
            <Button Text="Play" Command="{Binding PlayCommand}" />
            <Button Text="Shop" Command="{Binding ShopCommand}" />
            <Button Text="Settings" Command="{Binding SettingsCommand}" />
            <Button Text="How to Play" Command="{Binding HowToCommand}" />
        </VerticalStackLayout>

        <Label Grid.Row="2"
               Text="Swipe left/right to change lanes • Avoid cars • Collect coins &amp; powerups"
               TextColor="White"
               Opacity="0.7"
               HorizontalOptions="Center"
               FontSize="12" />

    </Grid>
</ContentPage>
